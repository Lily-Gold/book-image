<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Book Image" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- ファビコン設定 -->
    <link rel="icon" href="<%= asset_path('favicon.png') %>" type="image/png" sizes="512x512">
    <link rel="apple-touch-icon" href="<%= asset_path('favicon.png') %>" sizes="180x180">

    <%= yield :head %>

    <!-- フォントとスタイルシート -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link
      href="https://fonts.googleapis.com/css2?family=EB+Garamond:wght@400;700&family=Noto+Sans+JP:wght@400;500;700&family=Noto+Serif+JP:wght@400;700&display=swap"
      rel="stylesheet"
    />

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
  </head>

  <body>
    <!-- ヘッダー -->
    <header class="bg-[#004865] text-white h-14 flex items-center shadow-md">
      <div class="w-full flex justify-between items-center px-10 py-3">
        
        <!-- ロゴ＋アイコン -->
        <h1 class="text-2xl font-bold font-['EB_Garamond'] tracking-wide flex items-center">
          <%= link_to root_path, class: "flex items-center gap-1 hover:opacity-80 transition-opacity duration-200" do %>
            Book Image
            <%= image_tag "logo.png", alt: "Book Image アイコン", class: "h-10 w-auto object-contain translate-y-[1px]" %>
          <% end %>
        </h1>

        <!-- ナビゲーション -->
        <nav class="relative flex items-center gap-6 text-sm mr-2" data-controller="dropdown">
          <% if user_signed_in? %>
            <!-- プロフィールアイコン（クリックでメニュー開閉） -->
            <button
              data-action="click->dropdown#toggle"
              class="focus:outline-none"
            >
              <%= image_tag(
                    current_user.profile_image.presence || "avatar.png",
                    alt: "プロフィール画像",
                    class: "w-9 h-9 rounded-full object-cover border border-gray-300 hover:opacity-80 transition"
                  ) %>
            </button>

            <!-- ドロップダウンメニュー -->
            <div
              data-dropdown-target="menu"
              class="absolute right-0 mt-12 w-48 bg-white text-gray-700 rounded-md shadow-lg border border-gray-200 hidden"
            >
              <%= link_to "アカウント設定", edit_user_registration_path,
                  class: "block px-4 py-2 hover:bg-gray-100" %>
              <%= button_to "ログアウト", destroy_user_session_path, method: :delete,
                  class: "w-full text-left px-4 py-2 hover:bg-gray-100" %>
            </div>

          <% else %>
            <%= link_to "ログイン", new_user_session_path, class: "hover:underline" %>
            <%= link_to "新規登録", new_user_registration_path, class: "bg-[#986321] text-white px-4 py-2 rounded-md hover:opacity-80 transition-opacity duration-200 no-underline text-sm" %>
          <% end %>
        </nav>
      </div>
    </header>

    <!-- フラッシュメッセージ -->
    <!-- トースト通知 -->
    <%= render "layouts/flash" %>
    <%= turbo_stream_from "flash" %>
  <main class="min-h-[70vh]">
    <%= yield %>
  </main>

    <!-- フッター -->
    <footer class="bg-[#E0E0E2] text-[#004865] text-xs border-t border-gray-300 py-3 px-10">
      <div class="flex justify-between items-center">
        <div class="flex flex-wrap justify-center gap-4 text-[#004865] text-xs">
          <span class="relative after:content-[''] after:absolute after:right-[-8px] after:top-1/2 after:-translate-y-1/2 after:w-px after:h-3 after:bg-gray-400 last:after:hidden">
            利用規約
          </span>
          <span class="relative after:content-[''] after:absolute after:right-[-8px] after:top-1/2 after:-translate-y-1/2 after:w-px after:h-3 after:bg-gray-400 last:after:hidden">
            プライバシーポリシー
          </span>
          <span>お問い合わせ</span>
        </div>
        <p>© 2025 Book Image</p>
      </div>
    </footer>
  </body>
</html>
