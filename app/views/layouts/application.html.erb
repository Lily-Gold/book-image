<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Book Image" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- ✅ ファビコン設定 -->
    <link rel="icon" href="<%= asset_path('favicon.png') %>" type="image/png" sizes="512x512">
    <link rel="apple-touch-icon" href="<%= asset_path('favicon.png') %>" sizes="180x180">

    <%= yield :head %>

    <!-- ✅ フォントとスタイルシート -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=EB+Garamond:wght@400;700&display=swap" rel="stylesheet">

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
  </head>

  <body>
    <!-- ✅ ヘッダーここから -->
    <header class="bg-[#004865] text-white h-16 flex items-center shadow-md">
      <div class="w-full flex justify-between items-center px-10 py-3">
        
        <!-- 左側：ロゴ＋アイコン -->
        <h1 class="text-3xl font-bold font-['EB_Garamond'] tracking-wide flex items-center">
          <% if current_page?(root_path) %>
            <!-- トップページでは固定 -->
            <span class="flex items-center gap-1">
              Book Image
              <%= image_tag "favicon.png", alt: "Book Image アイコン", class: "h-12 w-12 object-contain translate-y-[1px]" %>
            </span>
          <% else %>
            <!-- それ以外のページではリンク化 -->
            <%= link_to root_path, class: "flex items-center gap-1 hover:opacity-80 transition-opacity duration-200" do %>
              Book Image
              <%= image_tag "favicon.png", alt: "Book Image アイコン", class: "h-12 w-12 object-contain translate-y-[1px]" %>
            <% end %>
          <% end %>
        </h1>

        <!-- 右側：ナビゲーション -->
        <nav class="flex gap-8 text-sm mr-2">
          <%= link_to "ログイン", "#", class: "hover:underline" %>
          <%= link_to "新規登録", "#", class: "hover:underline" %>
        </nav>
      </div>
    </header>
    <!-- ✅ ヘッダーここまで -->

    <%= yield %>
  </body>
</html>
