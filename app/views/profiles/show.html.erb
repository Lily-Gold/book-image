<div class="bg-[#F4F4F4] pt-[1.5rem] pb-6 min-h-[87vh]">

  <h2 class="text-3xl font-bold text-center text-[#004865] mb-8">
    マイページ
  </h2>

  <div class="max-w-5xl mx-auto px-1">

    <!-- プロフィールカード -->
    <div class="bg-white rounded-xl shadow-md p-2 flex items-center gap-6 mb-14">

      <div class="shrink-0">
        <% if current_user.avatar.attached? %>
          <%= image_tag current_user.avatar.variant(resize_to_fill: [120, 120]),
                class: "w-[120px] h-[120px] rounded-full object-cover" %>
        <% else %>
          <%= image_tag "avatar.png",
                class: "w-[120px] h-[120px] rounded-full object-cover" %>
        <% end %>
      </div>

      <div class="flex-1 mt-[-2px]">
        <p class="text-xl font-semibold text-[#004865]"><%= current_user.name %></p>
        <p class="text-sm text-gray-600 mt-1"><%= current_user.email %></p>

        <div class="flex gap-6 mt-6 text-sm text-gray-700">
          <span>登録日：<%= current_user.created_at.strftime('%Y/%m/%d') %></span>
        </div>
      </div>
    </div>


    <!-- ▼▼ タブ切り替えエリア ▼▼ -->
    <div data-controller="profile-tabs">

      <!-- ★ 全幅タブバー（シャドウ控えめ） -->
      <div class="flex mb-10 border border-[#CDD5DF] bg-white rounded-lg overflow-hidden w-full shadow-[0_1px_3px_rgba(0,0,0,0.06)]">

        <!-- 投稿したレビュー -->
        <button
          data-profile-tabs-target="tab"
          data-index="0"
          data-action="click->profile-tabs#switch"
          class="flex-1 px-4 py-2 text-center cursor-pointer
                 bg-[#004865] text-white text-sm font-medium">
          投稿したレビュー（<%= @reviews.count %>）
        </button>

        <div class="w-px bg-[#CDD5DF]"></div>

        <!-- ブックマーク -->
        <button
          data-profile-tabs-target="tab"
          data-index="1"
          data-action="click->profile-tabs#switch"
          class="flex-1 px-4 py-2 text-center cursor-pointer
                 bg-white text-gray-600 text-sm font-normal">
          ブックマーク
        </button>

        <div class="w-px bg-[#CDD5DF]"></div>

        <!-- 読書傾向 -->
        <button
          data-profile-tabs-target="tab"
          data-index="2"
          data-action="click->profile-tabs#switch"
          class="flex-1 px-4 py-2 text-center cursor-pointer
                 bg-white text-gray-600 text-sm font-normal">
          読書傾向
        </button>
      </div>


      <!-- ▼ パネル：投稿したレビュー -->
      <div data-profile-tabs-target="panel">
        <% if @reviews.empty? %>
          <div class="text-center py-16 text-gray-500 leading-relaxed">
            まだレビューがありません。<br>
            あなたの最初の“本の色”を投稿してみませんか？

            <div class="mt-6">
              <%= link_to "最初のレビューを投稿する", new_review_path,
                  class: "inline-block px-4 py-2 bg-[#986321] text-white rounded-lg text-sm hover:bg-[#7a4f1d] transition" %>
            </div>
          </div>
        <% else %>
        <% if @reviews.empty? %>
          <div class="text-center py-16 text-gray-500 leading-relaxed">
            まだレビューがありません。<br>
            あなたの最初の“本の色”を投稿してみませんか？

            <div class="mt-6">
              <%= link_to "最初のレビューを投稿する", new_review_path,
                  class: "inline-block px-4 py-2 bg-[#004865] text-white rounded-lg text-sm hover:bg-[#00354c] transition" %>
            </div>
          </div>
        <% else %>
          <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-2">
            <% @reviews.each do |review| %>
            <%= render "reviews/review_card_small", review: review %>
          <% end %>
        </div>
        <% end %>
        <% end %>
      </div>

      <!-- ▼ パネル：ブックマーク -->
      <div class="hidden text-center py-20 text-gray-500" data-profile-tabs-target="panel">
        準備中（ブックマーク機能ができたらここに表示）
      </div>

      <!-- ▼ パネル：読書傾向 -->
      <div class="hidden text-center py-20 text-gray-500" data-profile-tabs-target="panel">
        準備中（読書傾向グラフなどを予定）
      </div>

    </div>

  </div>
</div>
